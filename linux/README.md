# PiKVM Linux Launcher

This folder contains the Linux launcher for PiKVM.

It installs a freedesktop `.desktop` launcher and opens PiKVM in Chromium-compatible app mode.

## Install

From the repo root:

```bash
./linux/install.sh
